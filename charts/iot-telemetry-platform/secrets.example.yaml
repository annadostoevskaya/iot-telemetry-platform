# Reference Secret Template (DO NOT COMMIT REAL PASSWORDS)
# In production, use HashiCorp Vault, ExternalSecrets Operator, or SealedSecrets.
apiVersion: v1
kind: Secret
metadata:
  name: telemetry-secrets
  namespace: telemetry-system
type: Opaque
stringData:
  postgresql-password: "REPLACE_WITH_SECURE_PASSWORD"
  postgresql-postgres-password: "REPLACE_WITH_SECURE_ROOT_PASSWORD"
  redis-password: "REPLACE_WITH_SECURE_REDIS_PASSWORD"
  influxdb-token: "REPLACE_WITH_INFLUX_ADMIN_TOKEN"
  grafana-admin-user: "admin"
  grafana-admin-password: "REPLACE_WITH_SECURE_GRAFANA_PASSWORD"
